// collection names
const collection = Object.freeze({
  users: 'users',
});

// roles names
const roles = Object.freeze({
  user: 'user',
  admin: 'admin',
});

module.exports = {
  collection,
  roles,
}
